# Environment config
# ====================
env_type: codeArt # could be gym or codeArt
env_id: RacingAgent_v0
explore_render: 0 # bool 0: false, 1: true
eval_render: 0 # bool
debug_plot: 1 # bool, if true, the agent state is drawn as a 2d plot during the `get_obs()` call

# Training config
# ===================
# evaluation: 1 # bool
batch_size: 64
n_train_steps: 100 # per epoch
n_eval_steps: 100 # per epoch
n_rollout_steps: 500 # per epoch
n_total_timesteps: 1e6 # total number of steps that agent interacts with the env.
log_interval: 1
return_plot: 1 # bool
checkpoint_every_n_epoch: 5
# Critic config
# ===================
lr: 0.0001
critic_hidden_layers: [512, 512]
critic_activation: ReLU # "ReLU", "Sigmoid", "Tanh", "LeakyReLU", "ELU", "SELU", "GELU", "Softmax", "Softplus",
replay_buff_size: 1e6
tau: 0.1
gamma: 0.9
normalize_obs: 1 # bool
obs_rms_plot: 1 # bool, If true, the running avg of observation will be plotted
resume: 1 # bool, if true, training will be continued from the last saved checkpoint
resume_path: I:\projs\game-ai\results\dqn_RacingAgent_v0_2025-11-22_18-01-55 