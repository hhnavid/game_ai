# Each row structure:
# key \t type \t value \t #comment
#
# To comment a line, just put `#` at the start of it
#
run_name	str	ou_run
seed	int	0

# Environment config
# ====================
env_id	str	RacingAgent-v0

render	bool	False
render_eval	bool	False
evaluation	bool	True

layer_norm	bool	False	# layer normalization not implemented
normalize_observations	bool	True

batch_size	int	64	# per MPI worker

nb_train_steps	int	50	# per epoch cycle and MPI worker
nb_eval_steps	int	100	# per epoch cycle and MPI worker
nb_rollout_steps	int	100	# per epoch cycle and MPI worker
num_timesteps	int	5e6

noise_type	str	ou_0.2	# choices are ou_xx
param_noise_adapt_interval	int	0
plot_param_noise_dist	bool	False
return_plot	bool	True
checkpoint_every_n_epoch	int	100
# Actor/Critic config
# ===================
actor_lr	float	1e-4
critic_lr	float	1e-3

actor_hidden_layers	list_of_ints	64, 64	
critic_hidden_layers	list_of_ints	64, 64

critic_activation	str	relu
actor_activation	str	relu

replay_buffer_size	int	1e6

tau	float	1e-3
gamma	float	0.99
obs_rms_plot	bool	False	# If true, the running avg of observation will be plotted
reward_rms_plot	bool	False	# If true, the running avg of reward values will be plotted

# reward_scale	float	1.0
# normalize_returns	bool	False	# TODO: return normalization not implemented
# enable_popart	bool	False	# TODO: pop_art not implemented
# clip_norm	float	None                 # TODO: clip_norm not implemented
# critic_l2_reg	float	0	# TODO: critic_l2_reg not implemented
